Replace YOUR_API_KEY and Firebase values in firebase-init.js with your actual config.

Place a short audio file named splash-tune.mp3 in the same directory as your HTML file.

If you see "Firebase: Error (auth/invalid-api-key)", your Firebase API key or config is invalid.
Update the firebaseConfig object in your code with the correct values from your Firebase Console.

